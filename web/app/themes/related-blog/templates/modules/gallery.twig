<section class="module gallery wpb_animate_when_almost_visible wpb_fadeIn fadeIn wpb_start_animation animated"
  data-name="gallery"
  data-type="gallery">

  {% if title is defined and title is not empty %}
  <div class="gallery__top">
    <div class="container">
      <h2>{{ title }}</h2>
    </div>
  </div>
  {% endif %}

	<div class="gallery__items">
		<div class="container">

      {% for item in items %}
      <div class="gallery__item">

        {% if item.image is defined and item.image is not empty %}
        <figure>
          <a href="{{ item.link }}"><img src="{{ item.image }}" alt="{{ item.image_alt }}"></a>
        </figure>
        {% endif %}

      </div>
      {% endfor %}

    </div>
  </div>
</section>
